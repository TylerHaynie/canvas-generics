export declare class ImageDataUtility {
    private context;
    constructor(context: CanvasRenderingContext2D);
    createImageDataBySize(w: number, h: number): ImageData;
    createImageDataImageData(id: ImageData): ImageData;
}
