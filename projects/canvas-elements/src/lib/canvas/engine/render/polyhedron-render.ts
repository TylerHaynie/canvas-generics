export class PolyhedronRender {
// Get
//  - device: await adapter.requestDevice()
//  - context: canvas.getContext('webgpu') as GPUCanvasContext
//  - format: navigator.gpu.getPreferredCanvasFormat ? navigator.gpu.getPreferredCanvasFormat() : context.getPreferredFormat(adapter)

// configure context
//  - context.configure({ device, format, alphaMode: 'opaque'})


// create pipeline
//  - await device.createRenderPipelineAsync({ ... });
//      - vertex and fragment shaders are set here
//      - https://www.w3.org/TR/webgpu/#gpurenderpipeline

// - create buffers

}